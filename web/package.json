{
  "name": "quick-reading-web",
  "scripts": {
    "build": "parcel build ./index.html ./login.html --dist-dir ./dist",
    "build:watch": "parcel watch ./index.html ./login.html --dist-dir ./dist"
  },
  "devDependencies": {
    "parcel": "^2.10.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}